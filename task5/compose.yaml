#version: "3"
include:
  - docker-compose.yaml  #with registry declared
services:
  portainer:
    network_mode: host
    image: portainer/portainer-ce:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock